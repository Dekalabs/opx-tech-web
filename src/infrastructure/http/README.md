# Http

One way to persist data are API calls. In this folder we will have all the configuration related to http to make the API calls.

For example:

- Configuration of the http service
- Interceptors configuration
- Http errors
