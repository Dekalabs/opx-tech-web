# CONSTANTS

This directory contains the common constants of the UI layer that may want to share between the different components and views in our application.

For example:

- mask types for formatting
- 3rd-party adapters for our UI
- image sizes for UI visualization
- file max size and types
- regex
