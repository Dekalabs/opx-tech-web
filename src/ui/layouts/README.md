# LAYOUTS

The layout files will be placed inside this directory. 

When building a large single page application, there will be pages with same layouts. For instance, the login page and register page will have a different layout than home page and account page.
